# F-012 — Tasks

Format: `- [x] T-NN [P?] [US?] <description> (<exact file path>)`
Mode: **production** — phases, tests-first per story, small build units.
Paths are relative to the repo root. The catalog hook is the SSoT; its shipped
copy `.aspis/scripts/hooks/commitmsg.py` is regenerated by export in T-12.

## Phase 1 — Setup
Blocked by: nothing.
- [x] T-00 [P] Feature dir + SPEC/PLAN already scaffolded; confirm branch `feat/F-012-commit-msg-intel` and the active-feature pointer (`.aspis/current/active_feature.json`).

**Checkpoint**: feature live, pointer set.

## Phase 2 — Foundational (blocking)
Core refactor every story depends on; no story work until done.
- [x] T-01 [US1] Extract the attribution detection out of `validate` into one helper `find_attribution(message, convention) -> list[tuple[int, str]]`, leaving `validate`'s output identical (`src/aspis/data/catalog/scripts/hooks/commitmsg.py`).
- [x] T-02 [US1] Assert the refactor is behaviour-preserving — existing attribution tests still pass, plus a direct test of `find_attribution` (`tests/test_commitmsg.py`).

**Checkpoint**: one source of attribution detection, all existing tests green.

## Phase 3 — User Story 1 — Non-blocking auto-fix (priority: P1) 🎯 MVP
**Goal**: the commit-msg hook strips forbidden attribution and lets the commit proceed.
**Independent test**: feed a message with `Co-Authored-By:` → written file has it removed, hook exits 0.

Tests first (write failing, then implement):
- [x] T-03 [US1] Failing tests: attribution line stripped + reported; clean message byte-unchanged; `.claude`/`.opencode` domain mention preserved; hook exits 0 in all cases (`tests/test_commitmsg.py`).

Implementation:
- [x] T-04 [US1] Add pure `strip_attribution(message, convention) -> (cleaned, removed)` and wire `main()` to auto-fix → rewrite the message file → print `auto-fix: removed …` → validate the cleaned message → exit 0 (depends on T-03) (`src/aspis/data/catalog/scripts/hooks/commitmsg.py`).

**Checkpoint**: a commit with attribution is silently cleaned, never blocked.

## Phase 4 — User Story 3 — Config policy + escape hatch (priority: P3)
**Goal**: projects tune which auto-fixes run and can exempt a single commit.
**Independent test**: `autofix.attribution: false` ⇒ warn-not-strip; `Commit-Style: skip` ⇒ untouched.

Tests first:
- [x] T-05 [US3] Failing tests: policy `autofix.attribution: false` disables stripping (warns instead); a message carrying `Commit-Style: skip` passes unmodified and logs the exemption (`tests/test_commitmsg.py`).

Implementation:
- [x] T-06 [US3] Add the `autofix` block + `Commit-Style: skip` doc to `commit-convention.yaml`; read the policy/token via `_config` and honour both in the hook (depends on T-05) (`src/aspis/data/catalog/config/commit-convention.yaml`, `src/aspis/data/catalog/scripts/hooks/commitmsg.py`).

**Checkpoint**: auto-fix behaviour is data-driven; the rare exemption works.

## Phase 5 — User Story 2 — History audit & fix (priority: P2)
**Goal**: list (and, opt-in, repair) every commit whose message violates the convention.
**Independent test**: a repo with a known-bad historical message → audit names it; `--fix` rewrites it with content byte-identical.

- [x] T-07 [US2] Failing tests for `audit_history(root, convention)` — returns per-commit reports with violations; clean repo ⇒ empty (`tests/test_commits.py`).
- [x] T-08 [US2] Add shared `audit_history` (`src/aspis/commitaudit.py`) iterating `git log` messages through `validate` (depends on T-07).
- [x] T-09 [US2] Failing tests for `aspis commits --audit` (exit 1 + detail on findings, exit 0 clean) and `aspis commits --fix` (creates a backup ref, rewrites only flagged messages, asserts content byte-identical) (`tests/test_commits.py`).
- [x] T-10 [US2] Add the `commits` verb with `--audit` / `--fix`, registered in `COMMAND_MODULES` (`src/aspis/commands/commits.py`, `src/aspis/commands/__init__.py`) — `--fix` does backup-ref + content-identical reword (depends on T-08, T-09).
- [x] T-11 [US2] Add a read-only `commits` health line to `aspis doctor` using `audit_history` (`src/aspis/commands/doctor.py`, its test).

**Checkpoint**: `aspis commits --audit` proves history clean; `--fix` repairs safely; doctor surfaces drift.

## Phase 6 — Polish
- [x] T-12 Regenerate the shipped hook so `.aspis/scripts/hooks/commitmsg.py` == catalog; document the new verb + auto-fix in `README.md` / `docs/`; full gate sweep on Windows + Linux (`.aspis/scripts/hooks/commitmsg.py`, `README.md`).

## Dependencies & execution order
- Setup (T-00) → Foundational (T-01→T-02) → US1 (T-03→T-04) → US3 (T-05→T-06) → US2 (T-07→T-11) → Polish (T-12).
- US1 is the MVP — stop and validate after T-04 before continuing.
- Within a story: tests before implementation.
- US3 precedes US2 because the hook policy is small and keeps the auto-fix self-contained before the larger history work.

## Implementation strategy
- **MVP first**: T-00..T-04, then validate the auto-fix end-to-end (dogfood a commit with attribution).
- **Incremental**: add US3, then US2, each gated independently.
- Story 4 (learnable style) is **out of scope** — tracked for F-013.

## Build packets
`task-compile` turns each task into a self-contained packet at
`.aspis/features/F-012-commit-msg-intel/tasks/T-NN.md` from
`.aspis/templates/planning/TASK_PACKET.md`.
