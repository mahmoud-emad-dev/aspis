---
description: Report the project's current state — active feature, recent changes, where things stand.
agent: project-lead
---

Report where the project stands.

$ARGUMENTS

Answer from project intelligence, not by re-scanning the tree: read
`.aspis/context/CURRENT_STATE.md`, `.aspis/context/RECENT_CHANGES.md`, and
`.aspis/current/active_feature.json`. Summarize the active feature and its phase/mode,
what changed recently, and any open questions or blocking gates. Refresh the context
scripts first if the state looks stale.
