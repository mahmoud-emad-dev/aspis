# ASPIS START — generated by aspis init
# The brain keeps its own hygiene in .aspis/.gitignore (generated indexes,
# caches, traces). Project/stack ignores are managed below by `aspis gitignore`.
# ASPIS END
