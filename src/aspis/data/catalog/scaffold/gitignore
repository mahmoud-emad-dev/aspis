# ASPIS START — generated by aspis init
# The brain keeps its own hygiene in .aspis/.gitignore (generated indexes,
# caches, traces). Stack-specific ignores are managed below by `aspis gitignore`.
# This block is the universal baseline that is correct for any stack.

# OS / filesystem noise
.DS_Store
Thumbs.db
desktop.ini

# Local environment & secrets — never commit (keep an example file if you have one)
.env
.env.*
!.env.example

# Editor scratch files (share your editor config; ignore local scratch)
*.swp
*~

# ASPIS AI workspace & runtimes — versioned separately from your product (F-022).
# The brain (.aspis/) keeps its own git history; the runtime dirs are rendered from
# the catalog and tracked by content hashes + a change-log. None of this belongs in
# your product's git history (it would expose plans, context, and machine state).
.aspis/
{runtime_shields}
# ASPIS END

