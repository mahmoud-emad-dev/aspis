# {project_name} — Architecture (as-built)

<!--
This is the AS-BUILT architecture: what the project really is right now, recorded
as it gets built. It starts small and grows with each feature — real components,
real decisions, real changes. Build and review agents read THIS file to compare
what a feature needs against what already exists.

There are two architecture documents, with different jobs:

- INITIAL / intended architecture — your `docs/ARCHITECTURE.md` (or a root
  `ARCHITECTURE.md`): the plan you came in with (from ASPIS planning or an external
  source). It is fixed; only you change it. The planning lead reads it to decide the
  next feature. It says what you MEANT to build.
- AS-BUILT architecture — this file (`.aspis/context/ARCHITECTURE.md`): what was
  ACTUALLY built. Agents keep it current as decisions land. It is the source of
  truth for what exists today.

Keep this short and true. Update it when a feature changes the real shape of the
system — new component, new boundary, a decision that deviated from the intent.
-->

## Overview

_What this system is, in a few sentences. Filled at bootstrap / first feature._

## Components

_The real parts that exist today and how they connect. Grows as features land._

## Key decisions

_Notable as-built choices and deviations from the intended architecture, newest
first. Cross-reference `.aspis/context/DECISIONS.md` for the durable record._
