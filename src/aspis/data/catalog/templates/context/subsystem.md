# Subsystem: <name>

- **Status:** proposed
- **Created:** <date>   **Last reviewed:** <date>
- **One-liner:** <one sentence — what this subsystem is>

## Why it exists (problem)
<The pain without it; the problem it solves. A cold reader should get the point in 30 seconds.
This is intent, not implementation.>

## Responsibilities & boundaries
- **Owns:** <what this subsystem is responsible for>
- **Does NOT own (out of scope):** <what deliberately belongs elsewhere — names the boundary so the subsystem does not creep>

## Current behaviour (FIXED vs OPEN)
<Plain-language behaviour today: inputs → outputs, lifecycle. Mark **FIXED** (must not change)
vs **OPEN** (free to evolve). This is the section future sessions read first.>

## Integrations
<What it depends on, what depends on it, and what a change here ripples into.>

## System contracts (guarantees)
<What the rest of the system may assume is always true once this subsystem runs.>

## Future direction (optional)
<Where it is expected to go. Remove this section if there is nothing to say.>

## Changelog (append-only, newest last; ARCHITECTURE changes only)
<Only architectural change appends here — never bug fixes, refactors, renames, or perf.
Never rewrite a past line; supersede it with a new dated one.>
- <date> — Created.
