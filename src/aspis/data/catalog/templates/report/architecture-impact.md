# <feature-id> — Architecture Impact Report

> Recorded by planning when a feature changes a subsystem's intent (responsibilities,
> boundaries, lifecycle, integrations, or contracts — or adds/removes a subsystem).
> **NOT auto-applied:** project-lead confirms this with the user before any subsystem
> file under `.aspis/architecture/subsystems/` is updated.

## Affected subsystem(s)
- <subsystem name> — <EXISTING | NEW | RETIRE>

## Reason
<Why this change is needed now — the intent behind it, in plain language.>

## Summary of the architectural change
- **Current:** <how the subsystem is defined today (or "none" for a new subsystem)>
- **Proposed:** <the new intent / responsibility / boundary / contract / integration>
- **Fixed vs changed:** <what stays FIXED; what actually changes>

## Integration impact
<Which other subsystems this ripples into; which system contracts change; who must adapt.>

## Questions needing user confirmation
- <each question the user must answer before the subsystem file is updated>
