# Runtime definition — Cursor CLI. Detect-only for now (no transform adapter yet).
name: cursor
detect:
  exe: cursor
run: cursor
dir: .cursor
root_guide:
capabilities:
  mode: false
  subagents: false
  subagent_depth: 0
  exportable: false
